spotify_lookup.settings_form:
  path: '/admin/config/services/spotify-lookup'
  defaults:
    _form: '\Drupal\spotify_lookup\Form\SpotifySettingsForm'
    _title: 'Spotify Lookup Settings'
  requirements:
    _permission: 'administer site configuration'
